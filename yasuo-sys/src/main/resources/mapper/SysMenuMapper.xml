<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.codesensi.yasuo.sys.mapper.SysMenuMapper">

    <select id="listMenuPermsByRoles" resultType="cn.codesensi.yasuo.pojo.entity.SysMenu">
        SELECT m.*
        FROM sys_menu m
                 LEFT JOIN sys_role_menu rm ON rm.menu_id = m.id
                 LEFT JOIN sys_role r ON r.id = rm.role_id
        <where>
            AND r.`status` = 0
                  AND m.`status` = 0
                  AND r.`code` IN
            <foreach collection="roles" item="roleCode" open="(" separator="," close=")">
                #{roleCode}
            </foreach>
        </where>
    </select>

    <select id="listMenuByRoles" resultType="cn.codesensi.yasuo.pojo.entity.SysMenu">
        SELECT m.*
        FROM sys_menu m
                 LEFT JOIN sys_role_menu rm ON rm.menu_id = m.id
                 LEFT JOIN sys_role r ON r.id = rm.role_id
        <where>
            AND r.`status` = 0
                  AND m.`status` = 0
                  AND m.`type` <![CDATA[<>]]> 3
                  AND r.`code` IN
            <foreach collection="roles" item="roleCode" open="(" separator="," close=")">
                #{roleCode}
            </foreach>
        </where>
    </select>
</mapper>
